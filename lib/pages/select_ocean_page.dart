import 'package:flutter/material.dart';

class SelectOceanPage extends StatelessWidget {
  const SelectOceanPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("select Ocean"),
      ),
    );
  }
}
